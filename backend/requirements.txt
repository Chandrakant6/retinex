fastapi==0.115.6
uvicorn==0.32.1
python-multipart==0.0.20
opencv-python-headless==4.10.0.84
numpy==1.26.4
scipy==1.14.1
scikit-learn==1.5.2
tensorflow-cpu==2.18.0

# Notes:
# - tensorflow-cpu is enough for this MVP (small custom CNN, no GPU assumed
#   at rural PHC sites). Swap for `tensorflow` if you have a CUDA GPU and
#   move to a larger backbone.
# - If checkpoints/model.keras is missing or TF fails to import, the app
#   automatically falls back to a deterministic mock predictor — see
#   ml_model.py and doc/model.md.
